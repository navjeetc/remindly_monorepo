<!DOCTYPE html>
<html>
  <head>
    <title>Remindly - Caregiver Dashboard</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body class="bg-gray-50">
    <nav class="bg-white shadow-sm">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
          <div class="flex">
            <div class="flex-shrink-0 flex items-center">
              <h1 class="text-2xl font-bold text-blue-600">Remindly</h1>
            </div>
            <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
              <% if current_user %>
                <%= link_to "Dashboard", dashboard_path, class: "#{request.path == dashboard_path ? 'border-blue-500 text-gray-900' : 'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700'} inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium" %>
              <% end %>
              <%= link_to "How To", how_to_path, class: "#{request.path == how_to_path ? 'border-blue-500 text-gray-900' : 'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700'} inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium" %>
              <% if current_user %>
                <%= link_to "Contact Us", contact_path, class: "#{request.path == contact_path ? 'border-blue-500 text-gray-900' : 'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700'} inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium" %>
              <% end %>
              <% if current_user&.role_senior? %>
                <%= link_to voice_reminders_path, class: "#{request.path == voice_reminders_path ? 'border-blue-500 text-gray-900' : 'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700'} inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium" do %>
                  <svg class="mr-1 h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15.536 8.464a5 5 0 010 7.072m2.828-9.9a9 9 0 010 12.728M5.586 15H4a1 1 0 01-1-1v-4a1 1 0 011-1h1.586l4.707-4.707C10.923 3.663 12 4.109 12 5v14c0 .891-1.077 1.337-1.707.707L5.586 15z"/>
                  </svg>
                  Voice Reminders
                <% end %>
              <% end %>
              <% if current_user&.role_admin? %>
                <%= link_to "Users", admin_users_path, class: "border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium" %>
                <%= link_to "Audit Logs", admin_audit_logs_path, class: "border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium" %>
              <% end %>
            </div>
          </div>
          <div class="flex items-center space-x-4">
            <span class="text-xs text-gray-500 bg-gray-100 px-2 py-1 rounded">v<%= APP_VERSION %></span>
            
            <% if Rails.env.development? && current_user %>
              <!-- DEV ONLY: Quick User Switch -->
              <div class="relative">
                <select onchange="if(this.value) window.location.href='/dev/switch_user?user_id=' + this.value" 
                        class="text-xs bg-yellow-100 border border-yellow-300 text-yellow-800 rounded px-2 py-1 pr-6">
                  <option value="">🔧 <%= current_user.display_name %></option>
                  <optgroup label="Admins">
                    <% dev_users_by_role[:admins].each do |user| %>
                      <option value="<%= user.id %>">
                        <%= user.display_name %>
                      </option>
                    <% end %>
                  </optgroup>
                  <optgroup label="Caregivers">
                    <% dev_users_by_role[:caregivers].each do |user| %>
                      <option value="<%= user.id %>">
                        <%= user.display_name %>
                      </option>
                    <% end %>
                  </optgroup>
                  <optgroup label="Seniors">
                    <% dev_users_by_role[:seniors].each do |user| %>
                      <option value="<%= user.id %>">
                        <%= user.display_name %>
                      </option>
                    <% end %>
                  </optgroup>
                </select>
              </div>
            <% end %>
            
            <% if current_user %>
              <!-- Notifications Bell -->
              <% unread_count = current_user.notifications.unread.count %>
              <%= link_to notifications_path, class: "relative text-gray-700 hover:text-gray-900" do %>
                <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9"/>
                </svg>
                <% if unread_count > 0 %>
                  <span class="absolute -top-1 -right-1 inline-flex items-center justify-center px-2 py-1 text-xs font-bold leading-none text-white bg-red-600 rounded-full">
                    <%= unread_count %>
                  </span>
                <% end %>
              <% end %>
              
              <span class="text-sm text-gray-700"><%= current_user.display_name %></span>
              <%= link_to "Profile", profile_path, class: "text-sm text-gray-700 hover:text-gray-900" %>
              <%= button_to "Sign Out", logout_path, method: :delete, class: "inline-flex items-center px-3 py-2 border border-gray-300 shadow-sm text-sm leading-4 font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
            <% else %>
              <%= link_to "Sign In", login_path, class: "inline-flex items-center px-3 py-2 border border-transparent text-sm leading-4 font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <% if notice %>
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
        <div class="bg-green-50 border-l-4 border-green-400 p-4">
          <p class="text-sm text-green-700"><%= notice %></p>
        </div>
      </div>
    <% end %>

    <% if alert %>
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
        <div class="bg-red-50 border-l-4 border-red-400 p-4">
          <p class="text-sm text-red-700"><%= alert %></p>
        </div>
      </div>
    <% end %>

    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
      <%= yield %>
    </main>
  </body>
</html>
