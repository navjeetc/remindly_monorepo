<div class="space-y-6">
  <div class="flex justify-between items-center">
    <div>
      <h2 class="text-3xl font-bold text-gray-900">Tasks for <%= @senior.email %></h2>
      <p class="mt-1 text-sm text-gray-500">Manage appointments, errands, and activities</p>
    </div>
    <div class="flex space-x-4">
      <%= link_to "New Task", new_senior_task_path(@senior), class: "inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700" %>
      <%= link_to "← Back to Dashboard", senior_dashboard_path(@senior), class: "text-blue-600 hover:text-blue-800" %>
    </div>
  </div>

  <!-- Filters -->
  <div class="bg-white shadow sm:rounded-lg p-4">
    <%= form_with url: senior_tasks_path(@senior), method: :get, class: "grid grid-cols-1 gap-4 sm:grid-cols-5" do |f| %>
      <div>
        <%= f.label :view, "View", class: "block text-sm font-medium text-gray-700" %>
        <%= f.select :view, 
          options_for_select([
            ["All Tasks", ""],
            ["Upcoming", "upcoming"],
            ["Past", "past"]
          ], params[:view]),
          {}, 
          class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500 sm:text-sm" %>
      </div>

      <div>
        <%= f.label :status, "Status", class: "block text-sm font-medium text-gray-700" %>
        <%= f.select :status, 
          options_for_select([
            ["All Statuses", ""],
            ["Pending", "pending"],
            ["Assigned", "assigned"],
            ["In Progress", "in_progress"],
            ["Completed", "completed"],
            ["Cancelled", "cancelled"]
          ], params[:status]),
          {}, 
          class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500 sm:text-sm" %>
      </div>

      <div>
        <%= f.label :task_type, "Type", class: "block text-sm font-medium text-gray-700" %>
        <%= f.select :task_type, 
          options_for_select([
            ["All Types", ""],
            ["Appointment", "appointment"],
            ["Errand", "errand"],
            ["Activity", "activity"],
            ["Household", "household"],
            ["Transportation", "transportation"],
            ["Other", "other"]
          ], params[:task_type]),
          {}, 
          class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500 sm:text-sm" %>
      </div>

      <div>
        <%= f.label :priority, "Priority", class: "block text-sm font-medium text-gray-700" %>
        <%= f.select :priority, 
          options_for_select([
            ["All Priorities", ""],
            ["Low", "low"],
            ["Medium", "medium"],
            ["High", "high"],
            ["Urgent", "urgent"]
          ], params[:priority]),
          {}, 
          class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500 sm:text-sm" %>
      </div>

      <div class="flex items-end">
        <%= f.submit "Filter", class: "w-full inline-flex justify-center items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-gray-600 hover:bg-gray-700" %>
      </div>
    <% end %>
  </div>

  <!-- Tasks List -->
  <% if @tasks.any? %>
    <div class="bg-white shadow overflow-hidden sm:rounded-md">
      <ul role="list" class="divide-y divide-gray-200">
        <% @tasks.each do |task| %>
          <li>
            <%= link_to senior_task_path(@senior, task), class: "block hover:bg-gray-50" do %>
              <div class="px-4 py-4 sm:px-6">
                <div class="flex items-center justify-between">
                  <div class="flex items-center">
                    <!-- Task Type Icon -->
                    <div class="flex-shrink-0 mr-3">
                      <% case task.task_type %>
                      <% when "appointment" %>
                        <svg class="h-4 w-4 sm:h-5 sm:w-5 md:h-6 md:w-6 text-blue-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4"/>
                        </svg>
                      <% when "errand" %>
                        <svg class="h-4 w-4 sm:h-5 sm:w-5 md:h-6 md:w-6 text-green-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z"/>
                        </svg>
                      <% when "activity" %>
                        <svg class="h-4 w-4 sm:h-5 sm:w-5 md:h-6 md:w-6 text-purple-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.828 14.828a4 4 0 01-5.656 0M9 10h.01M15 10h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
                        </svg>
                      <% when "transportation" %>
                        <svg class="h-4 w-4 sm:h-5 sm:w-5 md:h-6 md:w-6 text-yellow-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7h12m0 0l-4-4m4 4l-4 4m0 6H4m0 0l4 4m-4-4l4-4"/>
                        </svg>
                      <% else %>
                        <svg class="h-4 w-4 sm:h-5 sm:w-5 md:h-6 md:w-6 text-gray-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"/>
                        </svg>
                      <% end %>
                    </div>

                    <div class="min-w-0 flex-1">
                      <p class="text-sm font-medium text-gray-900 truncate"><%= task.title %></p>
                      <p class="text-sm text-gray-500">
                        <%= task.scheduled_at.strftime("%b %d, %Y at %I:%M %p") %>
                        <% if task.location.present? %>
                          • <%= task.location %>
                        <% end %>
                      </p>
                    </div>
                  </div>

                  <div class="flex items-center space-x-2">
                    <!-- Priority Badge -->
                    <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium
                      <%= case task.priority
                          when 'urgent' then 'bg-red-100 text-red-800'
                          when 'high' then 'bg-orange-100 text-orange-800'
                          when 'medium' then 'bg-yellow-100 text-yellow-800'
                          else 'bg-gray-100 text-gray-800'
                          end %>">
                      <%= task.priority.titleize %>
                    </span>

                    <!-- Status Badge -->
                    <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium
                      <%= case task.status
                          when 'completed' then 'bg-green-100 text-green-800'
                          when 'in_progress' then 'bg-blue-100 text-blue-800'
                          when 'assigned' then 'bg-purple-100 text-purple-800'
                          when 'cancelled' then 'bg-gray-100 text-gray-800'
                          else 'bg-yellow-100 text-yellow-800'
                          end %>">
                      <%= task.status.titleize.gsub('_', ' ') %>
                    </span>

                    <% if task.assigned_to %>
                      <span class="text-xs text-gray-500">
                        Assigned to <%= task.assigned_to.email.split('@').first %>
                      </span>
                    <% else %>
                      <span class="text-xs text-gray-400 italic">Unassigned</span>
                    <% end %>
                  </div>
                </div>
              </div>
            <% end %>
          </li>
        <% end %>
      </ul>
    </div>

    <!-- Pagination -->
    <div class="flex justify-center">
      <%= paginate @tasks %>
    </div>
  <% else %>
    <div class="text-center py-12 bg-white rounded-lg shadow ipad-mini-fix">
      <svg class="mx-auto h-6 w-6 sm:h-8 sm:w-8 md:h-10 md:w-10 lg:h-12 lg:w-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"/>
      </svg>
      <h3 class="mt-2 text-sm font-medium text-gray-900">No tasks found</h3>
      <p class="mt-1 text-sm text-gray-500">Get started by creating a new task.</p>
      <div class="mt-6">
        <%= link_to "New Task", new_senior_task_path(@senior), class: "inline-flex items-center px-4 py-2 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700" %>
      </div>
    </div>
  <% end %>
</div>
