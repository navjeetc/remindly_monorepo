<p>
  Here is the printable daily routine sheet:
  <a href="<%= "#{ApplicationHelper::CANONICAL_HOST}/routine_sheet" %>">open and print it</a>.
  One page, big type, with room for medication times, meals, and the phone
  numbers worth having on the fridge door.
</p>

<p>
  About once a month I send one useful thing about caring for a parent at a
  distance, and a line on what has changed in Remindly. That is all — no more
  than that.
  <a href="<%= "#{ApplicationHelper::CANONICAL_HOST}/subscribers/unsubscribe/#{@subscriber.signed_id(purpose: :unsubscribe)}" %>">Unsubscribe</a>
  any time, or just reply to this email to stop.
</p>

<p>
  <a href="<%= ApplicationHelper::CANONICAL_HOST %>">Remindly</a> is free to use.
</p>
